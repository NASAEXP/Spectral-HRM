name: baselines.hrm_nocarry_bp_warmup@HierarchicalReasoningModel
head: lm_head@LMHead

half_layers: True

# HRM config
H_cycles: 2
L_cycles: 3
H_override: {}

bp_warmup_ratio: 0.2
bp_max_steps: 5

# Shared dense vocab matrix for embedding and LM head.
vocab_head:
  type: dense_tied
